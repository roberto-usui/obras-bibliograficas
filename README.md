# Obras Bibliográficas

#### Solução

Foi criado um script simples que recebe inputs pelo terminal.  
Dentro da pagina `ruby` rode `ruby main.rb` será pedido a quantidade de nomes e
depois os nomes separados por quebra de linha.  
O resultado será um output na tela.

Para teste unitários rode Rspec.
